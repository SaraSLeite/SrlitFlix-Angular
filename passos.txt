Configuração do GIT:
git config --global user.name SaraSLeite@gmail.com
git config --global user.email saraleite2708@gmail.com

Criar um projeto:
ng new Srlitflix

Executar o projeto:
cd Srlitflix
ng serve -o

Criar novos componentes:
ng g c pages/home
ng g c pages/search
ng g c pages/details

Criar um serviço:
ng g s services/movieApi




autoscroll.directive.scep.ts: Code

import { AutoScrollDirective } from './auto-scroll.directive';

describe('AutoScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoScrollDirective();
    expect(directive).toBeTruthy();
  });
});
